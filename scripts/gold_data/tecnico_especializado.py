TECNICO_ESPECIALIZADO = [
    {
        "doc_id": "GOLD-ESP-01",
        "test_type": "tecnico_especializado",
        "title": "Manifestos Declarativos YAML Avanzados en Kubernetes: Deployment y HorizontalPodAutoscaler",
        "content": "Contexto Técnico:\\nEn entornos cloud, el aprovisionamiento manual de contenedores es ineficiente y propenso a errores. En AyniKortex, toda la infraestructura se define de forma declarativa mediante manifiestos YAML que el Control Plane de Kubernetes reconcilia constantemente para garantizar la resiliencia operativa y la escalabilidad de nuestras APIs críticas.\\n\\nCódigo de Configuración K8s:\\n```yaml\\napiVersion: apps/v1\\nkind: Deployment\\nmetadata:\\n  name: aynikortex-auth-service\\n  namespace: production\\n  labels:\\n    app: auth-service\\n    tier: backend\\nspec:\\n  replicas: 3\\n  strategy:\\n    type: RollingUpdate\\n    rollingUpdate:\\n      maxSurge: 2\\n      maxUnavailable: 0\\n  selector:\\n    matchLabels:\\n      app: auth-service\\n  template:\\n    metadata:\\n      labels:\\n        app: auth-service\\n    spec:\\n      containers:\\n      - name: auth-api\\n        image: ecr.aws/aynikortex/auth-service:v3.2.1\\n        ports:\\n        - containerPort: 8080\\n        env:\\n        - name: SPRING_PROFILES_ACTIVE\\n          value: \\\"prod\\\"\\n        resources:\\n          requests:\\n            memory: \\\"512Mi\\\"\\n            cpu: \\\"250m\\\"\\n          limits:\\n            memory: \\\"1Gi\\\"\\n            cpu: \\\"1000m\\\"\\n        livenessProbe:\\n          httpGet:\\n            path: /actuator/health/liveness\\n            port: 8080\\n          initialDelaySeconds: 40\\n          periodSeconds: 15\\n```",
        "expected_category": "DevOps",
    },
    {
        "doc_id": "GOLD-ESP-02",
        "test_type": "tecnico_especializado",
        "title": "Pipeline Completo de Python para NLP: TfidfVectorizer y GridSearchCV con LogisticRegression",
        "content": "Contexto Analítico y Científico:\\nPara las tareas de procesamiento de lenguaje natural (NLP), Scikit-Learn proporciona un marco de trabajo excepcionalmente robusto. El siguiente script detalla el flujo de validación cruzada para ajustar los hiperparámetros de un pipeline analítico que une la vectorización TF-IDF avanzada de n-gramas con un clasificador Logístico utilizando solvers optimizados para datasets extensos y penalización elástica, lo cual garantiza una precisión (F1-score) notable ante el desbalance de clases.\\n\\nImplementación de Pipeline y Tuning:\\n```python\\nimport pandas as pd\\nfrom sklearn.pipeline import Pipeline\\nfrom sklearn.feature_extraction.text import TfidfVectorizer\\nfrom sklearn.linear_model import LogisticRegression\\nfrom sklearn.model_selection import GridSearchCV, StratifiedKFold\\nfrom sklearn.metrics import classification_report\\n\\ndef train_optimized_nlp_model(X_train: pd.Series, y_train: pd.Series):\\n    pipeline = Pipeline([\\n        ('tfidf', TfidfVectorizer(stop_words='english', max_df=0.9, min_df=5)),\\n        ('clf', LogisticRegression(class_weight='balanced', solver='saga', max_iter=2000))\\n    ])\\n    \\n    param_grid = {\\n        'tfidf__ngram_range': [(1, 1), (1, 2)],\\n        'tfidf__max_features': [10000, 25000],\\n        'clf__C': [0.1, 1.0, 10.0],\\n        'clf__penalty': ['l2', 'l1']\\n    }\\n    \\n    cv_strategy = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)\\n    \\n    grid_search = GridSearchCV(\\n        estimator=pipeline,\\n        param_grid=param_grid,\\n        cv=cv_strategy,\\n        scoring='f1_weighted',\\n        n_jobs=-1,\\n        verbose=2\\n    )\\n    \\n    grid_search.fit(X_train, y_train)\\n    print(f\\\"Mejores Hiperparámetros: {grid_search.best_params_}\\\")\\n    print(f\\\"F1-Score Validación Cruzada: {grid_search.best_score_:.4f}\\\")\\n    return grid_search.best_estimator_\\n```",
        "expected_category": "AI/ML",
    },
    {
        "doc_id": "GOLD-ESP-03",
        "test_type": "tecnico_especializado",
        "title": "Configuración HCL Avanzada en Terraform: Clúster RDS Multi-AZ y Storage Encryption con KMS",
        "content": "Contexto de Infraestructura Desplegada Automatizada:\\nLa infraestructura inmutable en entornos de Misión Crítica exige un control programático riguroso y versionado. El siguiente código de Terraform (Lenguaje HCL) ejemplifica el aprovisionamiento de una base de datos relacional robusta (Amazon RDS para PostgreSQL). El script asegura la alta disponibilidad Multi-AZ y obliga la criptografía de los volúmenes de datos en reposo mediante una llave simétrica gestionada por el servicio KMS de AWS, bloqueando el despliegue si se vulnera la compliance corporativa de seguridad de datos (Infosec).\\n\\nDeclaración Declarativa Terraform:\\n```hcl\\nresource \\\"aws_kms_key\\\" \\\"db_encryption_key\\\" {\\n  description             = \\\"KMS Master Key para cifrado de almacenamiento RDS Postgres\\\"\\n  enable_key_rotation     = true\\n  deletion_window_in_days = 30\\n  tags = { Environment = \\\"production\\\", Project = \\\"AyniKortex\\\" }\\n}\\n\\nresource \\\"aws_db_instance\\\" \\\"primary_database\\\" {\\n  identifier             = \\\"aynikortex-prod-db-cluster\\\"\\n  engine                 = \\\"postgres\\\"\\n  engine_version         = \\\"15.4\\\"\\n  instance_class         = \\\"db.r6g.large\\\"\\n  allocated_storage      = 250\\n  max_allocated_storage  = 1000\\n  storage_type           = \\\"io1\\\"\\n  iops                   = 3000\\n  \\n  db_name                = \\\"aynikortex_main\\\"\\n  username               = var.db_admin_username\\n  password               = var.db_admin_password\\n  \\n  multi_az               = true\\n  publicly_accessible    = false\\n  storage_encrypted      = true\\n  kms_key_id             = aws_kms_key.db_encryption_key.arn\\n  \\n  vpc_security_group_ids = [aws_security_group.db_sg.id]\\n  db_subnet_group_name   = aws_db_subnet_group.private_subnet_group.name\\n  \\n  backup_retention_period = 14\\n  skip_final_snapshot     = false\\n  final_snapshot_identifier = \\\"aynikortex-prod-db-final-snap\\\"\\n}\\n```",
        "expected_category": "Cloud",
    },
    {
        "doc_id": "GOLD-ESP-04",
        "test_type": "tecnico_especializado",
        "title": "Consultas SQL Avanzadas con CTEs, Window Functions y Particionamiento en PostgreSQL",
        "content": "WITH MonthlyDocumentStats AS (\\n    SELECT \\n        d.category,\\n        d.author_name,\\n        COUNT(d.document_id) AS total_documents,\\n        SUM(d.word_count) AS total_words,\\n        AVG(d.quality_score) AS avg_quality_score,\\n        DENSE_RANK() OVER (\\n            PARTITION BY d.category \\n            ORDER BY COUNT(d.document_id) DESC, AVG(d.quality_score) DESC\\n        ) AS rank_within_category\\n    FROM master_documents d\\n    WHERE d.created_date >= NOW() - INTERVAL '12 months'\\n      AND d.status = 'Approved'\\n    GROUP BY d.category, d.author_name\\n)\\nSELECT \\n    category,\\n    author_name,\\n    total_documents,\\n    total_words,\\n    ROUND(avg_quality_score::numeric, 2) AS quality_score\\nFROM MonthlyDocumentStats\\nWHERE rank_within_category <= 3\\nORDER BY category ASC, rank_within_category ASC; En el marco de la modernización de la plataforma, este componente fue refactorizado para garantizar un rendimiento óptimo bajo cargas de trabajo pesadas. El equipo de arquitectura técnica documentó cada decisión de diseño para asegurar que el despliegue a producción cumpla con todos los estándares de calidad ISO y las regulaciones corporativas vigentes de protección de datos (GDPR).",
        "expected_category": "Data Engineering",
    },
    {
        "doc_id": "GOLD-ESP-05",
        "test_type": "tecnico_especializado",
        "title": "Código HCL de Terraform para AWS S3 Bucket, Policiás IAM y Cifrado KMS",
        "content": "resource \\\"aws_kms_key\\\" \\\"data_lake_key\\\" {\\n  description             = \\\"KMS Key for encrypting Data Lake S3 Buckets\\\"\\n  deletion_window_in_days = 30\\n  enable_key_rotation     = true\\n\\n  tags = {\\n    Environment = \\\"Production\\\"\\n    Project     = \\\"AyniKortex\\\"\\n  }\\n}\\n\\nresource \\\"aws_s3_bucket\\\" \\\"data_lake_bucket\\\" {\\n  bucket        = \\\"aynikortex-data-lake-production-storage\\\"\\n  force_destroy = false\\n}\\n\\nresource \\\"aws_s3_bucket_server_side_encryption_configuration\\\" \\\"s3_encryption\\\" {\\n  bucket = aws_s3_bucket.data_lake_bucket.id\\n\\n  rule {\\n    apply_server_side_encryption_by_default {\\n      kms_master_key_id = aws_kms_key.data_lake_key.arn\\n      sse_algorithm     = \\\"aws:kms\\\"\\n    }\\n  }\\n}\\n\\nresource \\\"aws_s3_bucket_public_access_block\\\" \\\"block_public\\\" {\\n  bucket                  = aws_s3_bucket.data_lake_bucket.id\\n  block_public_acls       = true\\n  block_public_policy     = true\\n  ignore_public_acls      = true\\n  restrict_public_buckets = true\\n} En el marco de la modernización de la plataforma, este componente fue refactorizado para garantizar un rendimiento óptimo bajo cargas de trabajo pesadas. El equipo de arquitectura técnica documentó cada decisión de diseño para asegurar que el despliegue a producción cumpla con todos los estándares de calidad ISO y las regulaciones corporativas vigentes de protección de datos (GDPR).",
        "expected_category": "Cloud",
    },
    {
        "doc_id": "GOLD-ESP-06",
        "test_type": "tecnico_especializado",
        "title": "Custom Hook en TypeScript y React 18 para Peticiones HTTP con AbortController",
        "content": "import { useState, useEffect } from 'react';\\n\\ninterface FetchOptions {\\n  headers?: Record<string, string>;\\n  method?: 'GET' | 'POST' | 'PUT' | 'DELETE';\\n  body?: any;\\n}\\n\\ninterface FetchResponse<T> {\\n  data: T | null;\\n  loading: boolean;\\n  error: string | null;\\n}\\n\\nexport function useApiFetch<T>(url: string, options?: FetchOptions): FetchResponse<T> {\\n  const [data, setData] = useState<T | null>(null);\\n  const [loading, setLoading] = useState<boolean>(true);\\n  const [error, setError] = useState<string | null>(null);\\n\\n  useEffect(() => {\\n    const controller = new AbortController();\\n    const { signal } = controller;\\n\\n    async function executeFetch() {\\n      setLoading(true);\\n      try {\\n        const response = await fetch(url, {\\n          method: options?.method || 'GET',\\n          headers: { 'Content-Type': 'application/json', ...options?.headers },\\n          body: options?.body ? JSON.stringify(options.body) : undefined,\\n          signal,\\n        });\\n\\n        if (!response.ok) {\\n          throw new Error(`HTTP Error ${response.status}: ${response.statusText}`);\\n        }\\n\\n        const result: T = await response.json();\\n        setData(result);\\n        setError(null);\\n      } catch (err: any) {\\n        if (err.name !== 'AbortError') {\\n          setError(err.message || 'Ocurrió un error inesperado');\\n        }\\n      } finally {\\n        setLoading(false);\\n      }\\n    }\\n\\n    executeFetch();\\n\\n    return () => {\\n      controller.abort();\\n    };\\n  }, [url]);\\n\\n  return { data, loading, error };\\n} En el marco de la modernización de la plataforma, este componente fue refactorizado para garantizar un rendimiento óptimo bajo cargas de trabajo pesadas. El equipo de arquitectura técnica documentó cada decisión de diseño para asegurar que el despliegue a producción cumpla con todos los estándares de calidad ISO y las regulaciones corporativas vigentes de protección de datos (GDPR).",
        "expected_category": "Frontend",
    },
    {
        "doc_id": "GOLD-ESP-07",
        "test_type": "tecnico_especializado",
        "title": "Configuración de Seguridad en Spring Security 6 con Filtro de Validación JWT",
        "content": "package com.aynikortex.backend.security.config;\\n\\nimport com.aynikortex.backend.security.jwt.JwtAuthenticationFilter;\\nimport org.springframework.context.annotation.Bean;\\nimport org.springframework.context.annotation.Configuration;\\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\\nimport org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;\\nimport org.springframework.security.config.http.SessionCreationPolicy;\\nimport org.springframework.security.web.SecurityFilterChain;\\nimport org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;\\n\\n@Configuration\\n@EnableWebSecurity\\npublic class SecurityConfiguration {\\n\\n    private final JwtAuthenticationFilter jwtAuthFilter;\\n\\n    public SecurityConfiguration(JwtAuthenticationFilter jwtAuthFilter) {\\n        this.jwtAuthFilter = jwtAuthFilter;\\n    }\\n\\n    @Bean\\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\\n        http\\n            .csrf(csrf -> csrf.disable())\\n            .authorizeHttpRequests(auth -> auth\\n                .requestMatchers(\\\"/api/v1/auth/**\\\", \\\"/swagger-ui/**\\\", \\\"/v3/api-docs/**\\\").permitAll()\\n                .requestMatchers(\\\"/api/v1/admin/**\\\").hasRole(\\\"ADMIN\\\")\\n                .anyRequest().authenticated()\\n            )\\n            .sessionManagement(session -> session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))\\n            .addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class);\\n\\n        return http.build();\\n    }\\n} En el marco de la modernización de la plataforma, este componente fue refactorizado para garantizar un rendimiento óptimo bajo cargas de trabajo pesadas. El equipo de arquitectura técnica documentó cada decisión de diseño para asegurar que el despliegue a producción cumpla con todos los estándares de calidad ISO y las regulaciones corporativas vigentes de protección de datos (GDPR).",
        "expected_category": "Security",
    },
    {
        "doc_id": "GOLD-ESP-08",
        "test_type": "tecnico_especializado",
        "title": "Widget Flutter StatefulWidget en Dart con Animaciones Explícitas y Controllers",
        "content": "import 'package:flutter/material.dart';\\n\\nclass PulseAnimationCard extends StatefulWidget {\\n  final String title;\\n  final VoidCallback onTap;\\n\\n  const PulseAnimationCard({\\n    Key? key,\\n    required this.title,\\n    required this.onTap,\\n  }) : super(key: key);\\n\\n  @override\\n  State<PulseAnimationCard> createState() => _PulseAnimationCardState();\\n}\\n\\nclass _PulseAnimationCardState extends State<PulseAnimationCard> with SingleTickerProviderStateMixin {\\n  late AnimationController _controller;\\n  late Animation<double> _scaleAnimation;\\n\\n  @override\\n  void initState() {\\n    super.initState();\\n    _controller = AnimationController(\\n      duration: const Duration(milliseconds: 1200),\\n      vsync: this,\\n    )..repeat(reverse: true);\\n\\n    _scaleAnimation = Tween<double>(begin: 1.0, end: 1.05).animate(\\n      CurvedAnimation(parent: _controller, curve: Curves.easeInOut),\\n    );\\n  }\\n\\n  @override\\n  void dispose() {\\n    _controller.dispose();\\n    super.dispose();\\n  }\\n\\n  @override\\n  Widget build(BuildContext context) {\\n    return ScaleTransition(\\n      scale: _scaleAnimation,\\n      child: GestureDetector(\\n        onTap: widget.onTap,\\n        child: Card(\\n          elevation: 6.0,\\n          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16.0)),\\n          child: Padding(\\n            padding: const EdgeInsets.all(20.0),\\n            child: Text(\\n              widget.title,\\n              style: Theme.of(context).textTheme.titleMedium,\\n            ),\\n          ),\\n        ),\\n      ),\\n    );\\n  }\\n} En el marco de la modernización de la plataforma, este componente fue refactorizado para garantizar un rendimiento óptimo bajo cargas de trabajo pesadas. El equipo de arquitectura técnica documentó cada decisión de diseño para asegurar que el despliegue a producción cumpla con todos los estándares de calidad ISO y las regulaciones corporativas vigentes de protección de datos (GDPR).",
        "expected_category": "Mobile",
    },
    {
        "doc_id": "GOLD-ESP-09",
        "test_type": "tecnico_especializado",
        "title": "Script Bash Shell para Compilación, Etiquetado y Despliegue de Contenedores Docker",
        "content": "#!/usr/bin/env bash\\nset -euo pipefail\\n\\nREGISTRY_URL=\\\"123456789012.dkr.ecr.us-east-1.amazonaws.com\\\"\\nIMAGE_NAME=\\\"aynikortex-inference-engine\\\"\\nGIT_COMMIT=$(git rev-parse --short HEAD)\\nTIMESTAMP=$(date +%Y%m%d%H%M%S)\\nTAG=\\\"${GIT_COMMIT}-${TIMESTAMP}\\\"\\n\\necho \\\"==========================================\\\"\\necho \\\"📦 Compilando Imagen Docker: ${IMAGE_NAME}:${TAG}\\\"\\necho \\\"==========================================\\\"\\n\\ndocker build \\\\\\n  --build-arg BUILD_ENV=production \\\\\\n  --tag \\\"${IMAGE_NAME}:${TAG}\\\" \\\\\\n  --tag \\\"${IMAGE_NAME}:latest\\\" \\\\\\n  --file Dockerfile .\\n\\necho \\\"🔑 Autenticando con AWS ECR...\\\"\\naws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin \\\"${REGISTRY_URL}\\\"\\n\\necho \\\"🏷️ Etiquetando imágenes para ECR...\\\"\\ndocker tag \\\"${IMAGE_NAME}:${TAG}\\\" \\\"${REGISTRY_URL}/${IMAGE_NAME}:${TAG}\\\"\\ndocker tag \\\"${IMAGE_NAME}:latest\\\" \\\"${REGISTRY_URL}/${IMAGE_NAME}:latest\\\"\\n\\necho \\\"🚀 Subiendo imágen En el marco de la modernización de la plataforma, este componente fue refactorizado para garantizar un rendimiento óptimo bajo cargas de trabajo pesadas. El equipo de arquitectura técnica documentó cada decisión de diseño para asegurar que el despliegue a producción cumpla con todos los estándares de calidad ISO y las regulaciones corporativas vigentes de protección de datos (GDPR).",
        "expected_category": "DevOps",
    },
    {
        "doc_id": "GOLD-ESP-10",
        "test_type": "tecnico_especializado",
        "title": "Definición de Red Neuronal Convolucional Profunda en PyTorch nn.Module",
        "content": "import torch\\nimport torch.nn as nn\\nimport torch.nn.functional as F\\n\\nclass DeepTextClassificationCNN(nn.Module):\\n    def __init__(self, vocab_size: int, embed_dim: int, num_classes: int, num_filters: int = 128):\\n        super(DeepTextClassificationCNN, self).__init__()\\n        \\n        self.embedding = nn.Embedding(vocab_size, embed_dim, padding_idx=0)\\n        \\n        self.conv1 = nn.Conv1d(in_channels=embed_dim, out_channels=num_filters, kernel_size=3, padding=1)\\n        self.conv2 = nn.Conv1d(in_channels=embed_dim, out_channels=num_filters, kernel_size=4, padding=2)\\n        self.conv3 = nn.Conv1d(in_channels=embed_dim, out_channels=num_filters, kernel_size=5, padding=2)\\n        \\n        self.dropout = nn.Dropout(p=0.5)\\n        self.fc1 = nn.Linear(num_filters * 3, 256)\\n        self.fc2 = nn.Linear(256, num_classes)\\n\\n    def forward(self, text_input):\\n        embedded = self.embedding(text_input).permute(0, 2, 1)\\n        \\n        x1 = F.relu(self.conv1(embedded)).max(dim=2)[0]\\n        x2 = F.relu(self.conv2(embedded)).max(dim=2)[0]\\n        x3 = F.relu(self.conv3(embedded)).max(dim=2)[0]\\n        \\n        concatenated = torch.cat((x1, x2, x3), dim=1)\\n        dropped = self.dropout(concatenated)\\n        dense1 = F.relu(self.fc1(dropped))\\n        logits = self.fc2(dense1)\\n        \\n        return logits En el marco de la modernización de la plataforma, este componente fue refactorizado para garantizar un rendimiento óptimo bajo cargas de trabajo pesadas. El equipo de arquitectura técnica documentó cada decisión de diseño para asegurar que el despliegue a producción cumpla con todos los estándares de calidad ISO y las regulaciones corporativas vigentes de protección de datos (GDPR).",
        "expected_category": "AI/ML",
    }
]
